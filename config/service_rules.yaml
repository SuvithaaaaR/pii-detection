# Example service rules for necessity analysis
library_membership:
  required:
    - name
    - address
  exceptions:
    - pan
job_application:
  required:
    - name
    - address
    - dob
loan_application:
  required:
    - pan
    - name
    - address
    - dob
